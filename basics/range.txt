package main

import "fmt"

func main() {
   
	message := "Hello World!"

	for i, v:= range message {
		// v returns UniCode value
		fmt.Println(i, v)

		// v return rune now (rune is char)
		fmt.Printf("Index: %d, Rune: %c\n", i, v)
	}

}