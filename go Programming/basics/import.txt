package main

import (
	"fmt"
	foo "net/http" // named import for clarity
)
func main() {
	fmt.Println("Hello, World!")

	resp, err := foo.Get("http://example.com")
	if err != nil {
		fmt.Println("Error:", err)
		return
	}
	defer resp.Body.Close()

	fmt.Println("Response Status:", resp.Status)
}